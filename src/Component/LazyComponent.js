import React, { Component } from 'react'

export default class LazyComponent extends Component {
    render() {
        return (
            <div>
                This is lazy component,
                LazyComponentLazyComponentLazyComponentLazyComponentLazyComponentLazyComponent
                LazyComponent
                LazyComponent
                LazyComponent
                LazyComponent
                LazyComponent
                LazyComponent
                LazyComponent
                LazyComponent
                LazyComponent
                LazyComponentLazyComponent
                LazyComponent
                LazyComponent
                LazyComponent
                LazyComponent
                LazyComponent
                LazyComponent
            </div>
        )
    }
}
